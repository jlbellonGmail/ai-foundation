# Configuration

Configuration inputs consumed by the foundation runtime and validation tools.

Current contents:

* `prompts/` - system, domain and guardrail prompt contracts used by agents and
  runtime initialization flows.

Configuration files in this folder must be directly usable by scripts or runtime
code. Design notes belong in `docs/`.
