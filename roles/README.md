# Roles

Base role definitions used by foundation agents.

`AGENTS.md` is the active role index for repository-level behavior. Legacy hidden
role folders were moved to `_deprecated/2026-06-11/legacy-ai-roles/` because the
active product surface should be explicit and navigable.
